#name: Java CI
#
#on: [push]
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Build Docker image
#        run: docker build -t playwright-junit-test .
#
#      - name: Run tests
#        run: |
#          docker run --rm \
#            -e BASE_URL=https://the-internet.herokuapp.com \
#            -v ${PWD}/test.txt:/app/test.txt \
#            playwright-junit-test \
#            mvn test -Dtest=FileUploadTest

name: Docker Tests

on:
  workflow_dispatch:
  push:
    paths:
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'src/test/java/FileUploadTest.java'

jobs:
  docker-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t playwright-junit-test .

      - name: Run tests with debug
        run: |
          docker run --rm \
            -e BASE_URL=https://the-internet.herokuapp.com \
            -v ${PWD}/test.txt:/app/test.txt \
            playwright-junit-test \
            mvn test -Dtest=FileUploadTest -e -X